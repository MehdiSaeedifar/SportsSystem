<fieldset>
    <legend>رشته‌های ورزشی</legend>
    <div class="row">
        <div class="col-md-5" style="padding-top: 15px; padding-bottom: 20px;">
            <a class="btn btn-success" href="#/sport/add">
                <i class="fa fa-lg fa-plus"></i> افزودن رشته ورزشی
            </a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    فهرست رشته‌های ورزشی
                </div>
                <div class="table-responsive">
                    <table st-resize st-safe-src="sportsList" st-table="displayedSportsList" class="table table-bordered table-hover table-condensed th-center">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th><span st-sort="name" st-sort-default="true">نام رشته</span></th>
                            <th>عملیات</th>
                        </tr>
                        <tr>
                            <th></th>
                            <th>
                                <input st-search="name" placeholder="جستجو در نام رشته" class="input-sm form-control" type="search"/>
                            </th>
                            <th></th>
                        </tr>
                        </thead>
                        <tbody>
                        <tr ng-repeat="row in displayedSportsList" class="fx-fade-down">
                            <td>{{$index+1}}</td>
                            <td class="vert-align">{{row.name}}</td>
                            <td class="vert-align">
                                <a class="btn btn-primary btn-sm" href="#/sport/edit/{{row.id}}">
                                    <i class="fa fa-edit fa-lg"></i> ویرایش
                                </a>
                                <button class="btn btn-danger btn-sm" ng-click="removeSport(row,$index)">
                                    <i class="fa fa-trash-o fa-lg"></i> حذف
                                </button>
                            </td>
                        </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</fieldset>
<script type="text/ng-template" id="deleteSport.html">
    <div class="modal-header">
        <h3 class="modal-title"></h3>
    </div>
    <div class="modal-body">
        <div class="alert alert-danger">
            آیا از حذف رشته {{sport.name}} مطمئن هستید؟
        </div>

    </div>
    <div class="modal-footer">
        <button class="btn btn-primary" ng-click="cancel()">انصراف</button>
        <button class="btn btn-danger" ng-click="ok()">بله</button>
    </div>
</script>